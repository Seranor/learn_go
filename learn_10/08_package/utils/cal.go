package utils // 规范上，包名和当前文件所在目录名保持一致

func Add(x, y int) int {
	return x + y
}

func Mul(x, y int) int {
	return x * y
}
func Sub(x, y int) int {
	return x - y
}

func Div(x, y int) int {
	return x / y
}
